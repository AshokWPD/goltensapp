name: goltens_core
description: Common code to be shared across flutter projects
version: 1.0.0

environment:
  sdk: ">=3.0.2 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.9.0
  http_parser: ^4.1.2
  shared_preferences: ^2.2.0
  intl: ^0.18.1
  image_cropper: ^9.1.0
  pdf: ^3.10.1
  csv: ^6.0.0
  collection: ^1.17.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
